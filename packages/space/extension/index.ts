/**
 * What a space can do — the extension contract (`contract`) and the
 * bookkeeping that has to exist once there is more than one extension
 * (`registry`).
 */
export * from './contract.ts';
export * from './registry.ts';
