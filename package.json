{
  "name": "insession-sdk",
  "version": "0.0.0",
  "description": "InSession の SDK パッケージ群（@insession/ws-resilient-transport 等）を publish する pnpm workspace",
  "private": true,
  "license": "MIT",
  "type": "module",
  "//verify": "verify が『CI と同じ判定』の単一ソース。CI(.github/workflows/ci.yml)はこれを呼ぶだけで、個別チェックを列挙しない — 列挙すると package.json 側と静かに乖離するため（design-system の同コメント方針を踏襲）。",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "check": "biome check .",
    "verify": "pnpm typecheck && pnpm check && pnpm test",
    "format": "biome format --write .",
    "changeset": "changeset",
    "changeset:version": "changeset version"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@changesets/cli": "^2.31.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@10.12.1"
}
